# 複製成 .env 後再依本機環境調整：cp .env.example .env
#
# 此檔「只」供 docker-compose.yaml 做變數插值（${VAR}）使用，
# Compose 會自動讀取 compose 檔所在目錄的 .env，不需額外設定。
#
# 注意：這裡的變數不會注入到 apps/api 或 apps/web。
# 應用程式自己的環境變數請看 apps/api/.env.example 與 apps/web/.env.example，
# 兩邊的值必須手動保持一致（見下方各項說明）。

# --- PostgreSQL 容器初始化 ---
# 以下三項只在資料 volume「首次建立」時生效，用來初始化資料庫。
# 之後改動不會套用到既有資料，需先 docker compose down -v 清掉 volume 才會重新初始化。

# 需與 apps/api/.env 的 DB_NAME 一致
POSTGRES_DB=DB_NAME

# 需與 apps/api/.env 的 DB_USERNAME 一致
POSTGRES_USER=DB_USERNAME

# 需與 apps/api/.env 的 DB_PASSWORD 一致
# 必填，留空時 postgres 容器會拒絕啟動並回報：
#   Error: Database is uninitialized and superuser password is not specified.
# 產生方式：node -e "console.log(require('crypto').randomBytes(24).toString('base64url'))"
POSTGRES_PASSWORD=DB_PASSWORD

# 需與 apps/api/.env 的 DB_PORT 一致
POSTGRES_PORT=DB_PORT